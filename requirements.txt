# Orca — Phase 0 core dependencies (Python 3.11).
# Install:  uv pip install -r requirements.txt   (or pip)
pydantic>=2.6
pyyaml>=6.0
numpy>=1.26
pytest>=8.0
openai>=1.40        # shared LLM client (llm/client.py); OpenAI + W&B Inference
matplotlib>=3.8     # eval plots (eval/plots.py); imported lazily so run.py/pytest run without it

# Optional telemetry — the codebase runs and all tests pass without these
# (telemetry/weave_ops.py falls back to a local-JSONL / no-op logger).
# Install with:  uv pip install -r requirements-telemetry.txt
#   weave>=0.51
#   wandb>=0.16
