# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/.venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Orca run artifacts (local telemetry fallback, checkpoints)
runs/
wandb/
*.local.yaml

# Secrets — NEVER commit. Real keys live in .env (gitignored); .env.example is a
# committed placeholder. W&B tracing key also lives in ~/.netrc (outside repo).
.env
.env.*
!.env.example
*.key
secrets.*

# OS
.DS_Store
